⚠️ **Medium** — backend queries + frontend tabs UI

# 13. Enhanced Public Profiles

Upgrade `/u/{handle}` from a bare card to a full profile page with activity
tabs, karma, community memberships, and social links.

## Tasks

### A — Backend: enrich profile response

1. **Add social link fields to User model** — new nullable columns:
   - `github_username` (text)
   - `discord_username` (text)
   - `website_url` (text)
   Add via Alembic migration.

2. **Update `ProfileResponse`** schema — add:
   - `github_username`, `discord_username`, `website_url`
   - `post_count` (computed)
   - `comment_count` (computed)
   - `karma` (computed: sum of post scores + comment scores)
   - `communities` (list of `{slug, name, color}`)
   - `created_at` (already present)

3. **Update GET `/api/users/{handle}`** — join to compute:
   - `post_count`: `SELECT count(*) FROM posts WHERE author_id = user.id AND deleted_at IS NULL`
   - `comment_count`: same for comments
   - `karma`: `SELECT coalesce(sum(score), 0) FROM posts WHERE author_id = ...`
     plus same for comments
   - `communities`: join CommunityMembership → Community, return list

4. **Update `ProfileUpdateRequest`** — allow editing social link fields.
   Validate `website_url` with a URL regex (https only). GitHub/Discord
   are plain text (no URL needed).

5. **Add user activity endpoints:**
   - `GET /api/users/{handle}/posts?limit=20&offset=0` — paginated list of
     user's posts (with community slug/name). Exclude deleted.
   - `GET /api/users/{handle}/comments?limit=20&offset=0` — paginated list
     of user's comments (with post title + community slug). Exclude deleted.

### B — Frontend: enhanced profile page

6. **Redesign `/u/{handle}` page:**
   - **Header section:** avatar (lg), display name, handle, audience tag,
     bio, join date, karma badge
   - **Stats row:** posts · comments · karma · communities joined
   - **Social links row:** GitHub icon+link, Discord tag, website link
     (only show if set)
   - **Tab strip:** Posts | Comments
   - **Posts tab:** list of user's posts (title, community tag, score, date)
   - **Comments tab:** list of user's comments (body preview, post title
     link, score, date)

7. **Update profile settings** — add fields for social links:
   - GitHub username input
   - Discord username input
   - Website URL input

8. **Validation:** visit `/u/{handle}` → see full profile with stats,
   social links, and activity tabs. Edit social links in settings → they
   appear on public profile.

## Files created
- `backend/alembic/versions/xxx_add_social_links.py`
- `frontend/app/(app)/u/[handle]/ProfileTabs.tsx` + `.module.css`

## Files modified
- `backend/app/models/user.py` (add columns)
- `backend/app/profiles/schemas.py` (enrich response)
- `backend/app/profiles/router.py` (compute stats, add activity endpoints)
- `frontend/app/(app)/u/[handle]/page.tsx` (redesign)
- `frontend/app/(app)/u/[handle]/page.module.css` (redesign)
- `frontend/app/(app)/settings/profile/page.tsx` (add social fields)
