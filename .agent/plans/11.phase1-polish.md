⚠️ **Medium** — multiple areas, all straightforward

# 11. Phase 1 Polish

QA pass across frontend + backend: fix broken navigation, remove dummy data,
add responsive design, improve loading/error states, fix backend edge cases.

## Tasks

### A — Critical backend fixes
1. `votes/router.py` vote_comment: add null check on `post` after `session.get`
2. `comments/router.py` remove_comment: add null check on `post` after `session.get`
3. `votes/router.py`: check `removed_at` on posts/comments before allowing votes
4. `comments/router.py`: check parent `deleted_at`/`removed_at` before allowing replies

### B — Fix navigation (Sidebar)
1. Convert all `<button>` nav items to `<Link>` components
   - Feed → `/`
   - Explore → `/communities`
   - Notifications → remove (not implemented in Phase 1)
   - My profile → `/settings/profile`
2. Replace hardcoded `COMMUNITIES` array with API fetch of user's joined communities
3. Make community items `<Link href="/c/{slug}">` instead of buttons
4. Make sidebar a client component (needs API data + active route detection)
5. Highlight active nav item based on current route

### C — Fix navigation (Topbar)
1. Wire "New post" button → context-dependent: on community page link to `/c/{slug}/submit`, otherwise show a dropdown or link to `/communities`
2. Remove messages icon (Phase 3 feature, misleading to show now)
3. Remove ⌘K hint from search (not implemented). Keep search input as placeholder for visual continuity but disable or remove keyboard hint
4. Avatar already fixed in prior commit

### D — Responsive design
1. Shell: collapse sidebar on < 768px, show hamburger menu
2. Topbar: stack or hide search on mobile
3. Communities grid: 3 → 2 → 1 columns at breakpoints
4. Post cards / feed: reduce padding on mobile
5. Community landing page: stack meta stats vertically on mobile

### E — Loading & error states
1. Create a `PostCardSkeleton` component for feed loading
2. Add error state with retry button to home feed, community feed, comment section
3. Profile settings: skeleton loader instead of "Loading..."

### F — Miscellaneous cleanup
1. Post detail breadcrumb: `<a>` → `<Link>`
2. Remove fake notification badge "12" from sidebar
3. Ensure Topbar avatar handles missing user gracefully (already partially done)

## Validation
- All links navigate correctly (Playwright spot-check)
- Responsive: screenshot at 1920, 768, 375 widths
- Vote on removed post returns 403
- Reply to deleted comment returns 400
- No hardcoded dummy data remains in shipped components
