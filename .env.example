# Placeholder DSN — real connection wiring lands in Section 02 (feat/db).
DATABASE_URL=postgresql+psycopg://syntrix_app:dev@127.0.0.1:5432/postgres

# How the frontend reaches the backend (local dev).
BACKEND_URL=http://127.0.0.1:8000
